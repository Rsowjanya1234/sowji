pipeline {
    agent any
 

    stages {
        stage('Build') {
            steps {
                dir('PythonDockertest-EC2') {
                    sh 'ls'
                    sh 'docker build -t flask -f Dockerfile .'
                }
            }
        }
    }
}
