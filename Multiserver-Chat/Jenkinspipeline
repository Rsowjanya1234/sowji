pipeline {
    agent any

 

    stages {
        stage('Build') {
            steps {
                dir('Multiserver-Chat') {
                    sh 'ls'
                    sh 'docker build -t flask -f Dockerfile .'
                }
            }
        }
    }
}
