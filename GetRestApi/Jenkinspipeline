pipeline {
    agent any
 

    stages {
        stage('Build') {
            steps {
                dir('GetRestApi'){
                    sh 'ls'
                    sh 'docker build -t clientserver -f Dockerfile .'
                }
            }
        }
    }
}
