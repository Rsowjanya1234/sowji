pipeline {
    agent any
    environment {
        EC2_HOST = '52.62.94.91'
        EC2_USER = 'ubuntu'
    }
    stages {
        /*stage('Build') {
            steps {
                dir('GetRestApi') {
                    sh 'ls'
                    sh 'docker build -t flask -f Dockerfile .'
                }
            }
        }

 

        stage('Push to Docker Hub') {
            steps {
                sh "docker login -u sowjanyarangisetty5@gmail.com -p 'Rsowjanya@16'"
                sh 'docker tag flask sowjanyarangisetty/python-programs:flask'
                sh "docker push sowjanyarangisetty/python-programs:flask"
            }
        }*/

 

        stage('Pull Docker Image on EC2') {
            steps {
                script {
                    withCredentials([file(credentialsId: 'pemkey', variable: 'PRIVATE_KEY_FILE')]) {
                        sh """
                            chmod 400 \$PRIVATE_KEY_FILE
                            ssh -i \$PRIVATE_KEY_FILE -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \$EC2_USER@\$EC2_HOST<< EOF
                            //ssh -i your-ssh-key.pem -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@your-ec2-host 'sudo docker pull your-image:tag'

 

                            sudo docker login -u sowjanyarangisetty5@gmail.com -p 'Rsowjanya@16'
                            sudo docker pull sowjanyarangisetty/python-programs:flask
                            
                           EOF
                        """
                    }
                }
            }
        }
    }
}
